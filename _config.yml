title: Rubber Ducking Club
# Shown in the theme header on every page, and used as the tagline in <title>.
# Per-page `description:` front matter overrides it for meta/link-preview text.
description: Maieutic Rubber Duck On Call
url: https://rubberducking.club
lang: en

# The hacker theme gates its .zip/.tar.gz buttons on this.
show_downloads: false

remote_theme: pages-themes/hacker@v0.2.0
plugins:
- jekyll-remote-theme
- jekyll-seo-tag # required: the theme's default layout calls {% seo %}
- jekyll-sitemap
